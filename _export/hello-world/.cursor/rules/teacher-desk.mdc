---
description: 習作台（teacher-desk）手機／桌面通則
globs:
  - directory/apps/teacher-desk/**
  - scripts/teacher-desk*
  - scripts/install-teacher-desk.ps1
  - scripts/README-teacher-desk.md
---

# 習作台

- 介面、說明、toast、對話框、commit 一律**繁體中文**（zh-Hant）；品牌名可保留「PDF」等通用檔案格式。
- 管道選項顯示「通訊軟體班級群組／Google 教室／雲端…」，勿用英文 Classroom。
- 掃描來源寫「掃描王」，勿只寫 CS。
- 只用座號，禁止存學生姓名。
- 手機 PWA：`directory/apps/teacher-desk/`；桌面：`scripts/teacher-desk-app.ps1`。
- 狀態檔：`班級狀態.json`（相容 `class-state.json`）。
- 掃描命名：`座號-R次數.ext`（例 `05-R01.pdf`）。
- 桌面資料夾：`習作台資料\掃描匯入` →「處理掃描匯入」→ `練習回傳`。
- 與「習作批改」分工：批改產練習（國中課本形式）；習作台掌握發送與回傳。
- 群發／回傳文案採國中課本步驟：一、先看這裡 → 二、影片 → 三、練習 → 四、解答。之後所有輸出皆比照。
- 可從同一預覽網址的「習作批改」同步程度（localStorage `math-grader-v1`）。
- 無裝置：提供「複製需列印座號」；正式整班列印包仍可用電腦習作批改。
