---
description: 不要把桌面／下載／文件／圖片直接搬到大容量碟
alwaysApply: true
---

# 不要再歸檔到大容量碟

20260717 曾把桌面／下載／文件／圖片搬到 `D:\` 或 `F:\` 的「桌面歸檔／下載歸檔／文件歸檔／圖片歸檔」。**此動作已停止，不要再做。**

禁止：
- 新增或執行「直接搬移到大容量碟」的腳本
- 建立 `F:\桌面歸檔`、`F:\下載歸檔`、`F:\文件歸檔`、`F:\圖片歸檔` 當自動存放目標
- 把 Cursor／瀏覽器預設下載路徑改成大容量碟歸檔夾

允許：
- 只把**已在歸檔夾裡的檔**搬回系統原位置（桌面、下載、文件、圖片）
- 使用者明確指定單一檔案要放到哪個資料夾時，才搬那一個檔
