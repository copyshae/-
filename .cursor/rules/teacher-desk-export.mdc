---
description: 習作台改動經由此倉庫匯出到 hello-world
alwaysApply: false
---

# 習作台匯出

- 正式程式在 `copyshae/hello-world` 的 `directory/apps/teacher-desk` 與 `scripts/teacher-desk*`。
- 本倉庫（`copyshae/-`）以 `_export/hello-world/` 暫存待套用變更。
- 套用：`_export/hello-world/apply-to-hello-world.ps1`（需本機 Desktop\hello-world）。
- 介面與 commit 用繁體中文；只用座號、不存姓名。
