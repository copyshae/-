# 工作日誌｜習作台手機可見性與桌面啟動

日期：2026-08-15

## 問題

1. 手機上沒看到程式
2. 桌面 `.vbs` 開不起來；`.cmd` 一開就沒

## 結論

### 手機

習作台是 GitHub Pages PWA，不是商店 App。需 Safari／Chrome「加入主畫面」，圖示名「習作台」。

### 桌面

舊安裝器用 `Set-Content -Encoding ASCII` 寫入含中文路徑的 `.cmd`／`.vbs`，路徑變成 `?????`，啟動失敗且視窗一閃即關。

## 修復

- 內部路徑改 `TeacherDeskApp`／`TeacherDeskData`（ASCII）
- 啟動器內容純 ASCII；失敗會 pause + `TeacherDesk-error.txt`
- 匯出：`_export/hello-world/`（此 Agent 無法直推 hello-world）

## 請使用者做

```powershell
powershell -ExecutionPolicy Bypass -File .\_export\hello-world\apply-to-hello-world.ps1
```

然後雙擊 **`TeacherDesk-start.cmd`**。
