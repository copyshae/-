# 工作日誌｜習作台手機可見性

日期：2026-08-15

## 問題

手機上沒看到「此程式」（習作台）。

## 結論

習作台是 **GitHub Pages 網頁 App（PWA）**，不是 App Store／Play 商店安裝檔。
未「加入主畫面」前，手機桌面不會出現圖示；圖示名稱是 **「習作台」**。

## 立刻作法

1. 用 Safari（iPhone）或 Chrome（Android）打開：
   https://copyshae.github.io/hello-world/directory/apps/teacher-desk/
2. 分享／選單 → **加入主畫面**／**加到主畫面**
3. 回到桌面找綠色圖示「習作台」
4. 若在 LINE、Cursor 內建瀏覽器開啟，通常沒有加入選項 → 改用系統瀏覽器

## 程式修正（待套用到 hello-world）

路徑：`_export/hello-world/`

- 頁首清楚寫出「加入主畫面」步驟
- Android `beforeinstallprompt` 安裝按鈕
- 已安裝（standalone）時自動隱藏提示
- manifest 圖示 `any`／`maskable` 分開
- service worker 快取升到 `teacher-desk-v4`
- 目錄與 README 補「找不到圖示」排除說明

套用：`_export/hello-world/apply-to-hello-world.ps1`
