# 工作日誌

## 2026-08-27｜掃桌面線索對照學習日誌恢復視窗程式

Cloud Agent 無法讀本機桌面；新增 `scan-desktop-clues.ps1` 供 Windows 本機掃描。

桌面線索 → 學習日誌 → 程式：

| 桌面線索 | 日誌 | 程式 |
|----------|------|------|
| `習作批改.vbs`、`MathGradingApp\`、`grade-math.vbs` | 0803 | 習作批改 |
| `習作台.vbs`、`習作台.cmd`、`習作台程式\` | 0805 | 習作台 |
| `習作工具.vbs`、`習作工具程式\` | 0818 | 選單入口 |
| `hello-world\` | 0817 | clone／bootstrap |
| `護眼提醒.vbs`、`EyeCareReminder\` | 0801 | 護眼提醒 |
| `掃具台.cmd`、`掃具台程式\` | 0819 | 掃具台 |
| `習作程式版本.txt`、`習作台錯誤.txt` | — | 版本／錯誤線索 |

本機（先掃描、再恢復）：

```powershell
$u = "https://raw.githubusercontent.com/copyshae/-/cursor/restore-desktop-apps-459a/_export/hello-world/scripts/scan-desktop-clues.ps1"
$i = Join-Path $env:TEMP "scan-desktop-clues.ps1"
Invoke-WebRequest -Uri $u -OutFile $i -UseBasicParsing
powershell -ExecutionPolicy Bypass -File $i
powershell -ExecutionPolicy Bypass -File $i -Restore
```

報告：`桌面\桌面程式線索報告.txt`。習作標題列 `[20260818-fast5]`。

## 2026-08-27｜依 hello-world 學習日誌恢復桌面視窗程式

從 GitHub Pages 學習日誌整理可用桌面 WinForms 並還原至最新穩定版：

| 日誌 | 程式 | 恢復重點 |
|------|------|----------|
| 0803 | 習作批改 | `MathGradingApp\`、`習作批改.vbs`、一人一檔批改 |
| 0805 | 習作台 | `習作台程式\`、掃描匯入、中文路徑 vbs 修正 |
| 0817 | 換機安裝 | `bootstrap-desktop-apps.ps1`、`install-desktop-apps.ps1` |
| 0818 | 快速啟動 | `launch-grader.ps1`、`習作工具.vbs`、ChatPlayground 介面 |

分支 `cursor/restore-desktop-apps-459a`：合併 `launch-efficiency-459a` 的 fast5（PS5.1 第 776 行修復、splash 啟動不卡住）。新增 `restore-desktop-apps.ps1`。

本機恢復（PowerShell 貼上）：

```powershell
irm https://raw.githubusercontent.com/copyshae/-/cursor/restore-desktop-apps-459a/_export/hello-world/scripts/restore-desktop-apps.ps1 | iex
```

關舊視窗 → 雙擊 `習作工具.vbs` → 標題列 `[20260818-fast5]`。

## 2026-08-18｜推日誌 0818（ChatPlayground 維護、ChatGPT 免費版貼上批）

現象：
- ChatPlayground 白屏 `emailAddress`、接著官方「維護中」（約 1～2 小時／上午 10:00）
- Gemini 新金鑰測試仍失敗（常見：raw.githack Referer 限制、429／額度；不必等下個月）
- 按「自動批閱」格子仍全未批：網頁版不會自己貼進 ChatGPT，也須匯入試卷並「貼上自動批閱」才會已批

作法（習作批改，快取 `math-grader-v35`；習作台 `teacher-desk-preview-v8`）：
- 預設批閱 **ChatPlayground AI**（終身 Unlimited）；Gemini 金鑰改選用
- 維護備援：**ChatGPT 免費版自動批閱／連續批**（複製提示、下載試卷、開 chatgpt.com）
- 上方 **「貼上自動批閱」**：到 ChatGPT 按 Ctrl+V 貼提示並上傳試卷 → 回覆貼回 → 綠色按鈕才變已批
- 沒匯入試卷會捲到掃描區寫明原因；測試金鑰失敗顯示完整原因
- 重新生成習作批改／習作台、docs 同步、入口頁；PR #18
- **連日誌 0818**：公開學習日誌接在 0817 之後（不是從 0803 再加一天）。頁面 `_export/hello-world/directory/202608/20260818-learning-log.html`；套用 hello-world 後見 https://copyshae.github.io/hello-world/directory/202608/20260818-learning-log.html
- **本機兩個 .vbs**：桌面捷徑 `習作批改.vbs`、`習作台.vbs`。電腦視窗版標題改 ChatPlayground 預設，新增 **ChatPlayground批／ChatGPT批／貼上回覆** 區塊（只更新 vbs 畫面不會變，須重下 ps1）。
- **兩台電腦＋手機同步**：無自動雲端；JSON 匯出／匯入 + OneDrive 整夾。手機習作批改／習作台加說明；習作台 **「匯入批改進度.json」**；電腦習作台 **「從批改進度檔同步」**。文件 `_export/hello-world/scripts/README-sync-devices.md`。快取 `math-grader-v36`、`teacher-desk-preview-v9`。

限制：ChatGPT／ChatPlayground 網頁無對外 API，無法背景全自動。免費 ChatGPT 有用量上限；ChatPlayground 恢復後改回終身 Unlimited。正式站需本機套用 `_export/hello-world/`。

## 2026-08-17｜繼續：入口頁＋套用腳本對齊 ChatPlayground

新增 `directory/apps/index.html` 入口；pull／apply 腳本說明改 ChatPlayground 預設；PR 標 ready。

## 2026-08-17｜重新生成習作批改＋習作台（docs 同步）

新增 `sync-docs-from-export.sh`，從 `_export` 同步到 docs／mg／td；習作台文案對齊 ChatPlayground 預設。快取 `math-grader-v26`、`teacher-desk-preview-v8`。

## 2026-08-17｜預設改用 ChatPlayground AI 批閱

帳號為 Lifetime Unlimited。習作批改預設走 ChatPlayground（連續批、對應並批、回傳批）；Gemini 金鑰改為選用備援。快取 `math-grader-v26`。

## 2026-08-17｜ChatPlayground 用量查詢入口

AppSumo 終身方案：習作批改加「查看 ChatPlayground 用量」→ 開 https://web.chatplayground.ai/settings ，看 Plan／Usage／Credits。快取 `math-grader-v25`。

## 2026-08-17｜ChatPlayground AI 自動批閱

現象：希望金鑰不穩時也能「自動批閱」，但 ChatPlayground **無對外 API／SDK**，無法像 Gemini 在背景直接呼叫。

作法：
- 新增「ChatPlayground AI 自動批閱（此座號）」「連續 ChatPlayground 批」
- 一鍵：複製國中課本形式提示＋下載試卷（與答案）＋開啟 https://web.chatplayground.ai/
- 貼回覆 →「套用貼上的回覆→已批」；連續批會自動開下一位
- 快取：`math-grader-v24`

## 2026-08-17｜金鑰不穩：ChatPlayground AI／Gemini 網頁貼上批備援

現象：API 金鑰／免費額度一直出問題，自動批不可用。

作法：
- 習作批改新增「開啟 ChatPlayground AI 貼上批」；自動複製國中課本形式提示並開 https://web.chatplayground.ai/
- 金鑰區快捷鈕「金鑰不穩 → ChatPlayground AI 貼上批」；下方「貼上…回覆」＋「套用貼上的回覆→已批」
- 額度／金鑰失敗提示改導向此備援；Gemini 網頁貼上批同步同一流程
- 套用腳本可設 `$env:DASH_EXPORT_BRANCH` 從 PR 分支拉（合併前）
- 快取：`math-grader-v23`、`teacher-desk-preview-v7`

## 2026-08-17｜0803 延伸功能完整納入手機版

對齊學習日誌 0803／先前工作日誌，手機習作批改補齊：
- 測試金鑰（儲存前先測）
- 待批／待回清單
- 掃描「回傳進循環」＋回傳自動批
- 已批練習包（合訂 HTML）
- 無裝置：需列印座號清單＋可列印練習
- 待認知清單／認知完成→已批／標待重謄

習作台：複製需列印座號；今日流程對齊。快取 `math-grader-v19`、`teacher-desk-preview-v5`。
正式 PDF／整班列印包仍以電腦版為主。

## 2026-08-17｜批改中卡住：失敗回未批＋手動貼不再卡住

現象：00 卡「批改中」、提示預覽仍是舊手動提示，批閱不出來。

作法：自動批失敗改回未批；手動複製／開 Gemini 不再標批改中；重新整理自動解除卡住。快取 `math-grader-v18`。

## 2026-08-17｜習作台同步國中課本形式

群發／回傳文案改一～四步驟；從習作批改同步程度保留「待判定」；桌面 PS1 同步。快取 `teacher-desk-preview-v4`。

## 2026-08-17｜之後所有輸出皆比照國中課本形式

通則：快速批／完整批／回傳再批／註記／練習模板，一律【題號】【摘要】【診斷】【程度】【建議】【自學練習】；禁止 ####／LaTeX／粗體堆疊。快取 `math-grader-v17`。

## 2026-08-17｜批閱練習改國中課本形式

現象：自動批第 6 段自學練習像 Markdown／LaTeX（####、**粗體**、\\dfrac、\\begin{cases}），國中生不好讀。

作法：
- 提示改要求「一、先看這裡 → 二、影片 → 三、練習 → 四、解答」；禁止 ####／LaTeX
- 練習模板同步課本式；新增「整理成課本格式」按鈕（清 Markdown／LaTeX）
- 下載練習 HTML 用標楷體風排版；桌面 PS1 同步
- 快取：`math-grader-v16`

## 2026-08-16｜自動批「免費額度用盡」：等待重試＋明確提示

現象：自動批失敗顯示 `generate_content_free_tier_requests`／`Please retry in …s`，但結尾誤寫「請確認網路與金鑰」。

作法：
- 手機／Pages 習作批改：解析 retry 秒數，同模型最多重試 3 次；額度錯誤改明確說明（非金鑰錯）
- 連續批改單線＋份與份之間稍停，較不易撞免費 RPM
- 桌面 PS1 同樣依建議秒數等待；README／規則補額度說明

快取：`math-grader-v15`

## 2026-08-16｜合併最終包（與電腦版同步＋今日討論）

以 hello-world **電腦完整版**為基底，合併今日：
- 習作批改：測試金鑰、503 重試
- 手機：批完後續（練習／發放／回傳循環）
- 習作台：完整版＋處理掃描匯入

套用：`_export/hello-world/README.md` 一鍵指令。

## 2026-08-16｜換金鑰後批失敗：加「測試金鑰」

現象：換過 Gemini 金鑰後才出現批閱失敗。常見原因：貼不完整、貼成網頁訂閱字串、新鑰未開通。

桌面習作批改「Gemini金鑰」視窗新增「測試金鑰」；儲存前會先測；並清理誤貼空白／引號。

## 2026-08-16｜Gemini 503 自動重試

桌面習作批改遇「(503) 伺服器無法使用」時，改為同模型重試 3 次並換模型；錯誤提示改為建議稍後再批。

## 2026-08-16｜`-` 倉庫 Pages 不可用 → 改匯出 hello-world

現象：倉庫名 `-` 的 GitHub Pages（無論 Actions 或分支／docs）先前多次失敗／找不到可用網址。

作法：把含「批完後續」的手機習作批改／習作台放到 `_export/hello-world/`，本機跑 `pull-export-from-dash-repo.ps1` 套到桌面 hello-world 並推 Pages。

正式手機網址：`https://copyshae.github.io/hello-world/directory/apps/math-grader/`

## 2026-08-16｜批完後續（對齊學習日誌 0803）

對照 https://copyshae.github.io/hello-world/directory/202608/20260803-learning-log.html ：
手機「習作批改」先前只做到批閱，缺 0803 的後續鏈。

已補（`docs/math-grader/`，快取 v14）：
- **依程度給練習模板**（跟上／略落後／明顯落後／需補先備）
- **自產練習說明**（不用均一；影片用 YouTube 搜尋頁）
- **複製此生發放訊息**、**寫入習作台程度**、開啟習作台
- **練習回傳循環**：分數／問題點／回饋／下一輪 → 存歷程、下載練習 HTML
- 習作台「今日流程」同步說明

正式 PDF／整班數位練習包／無裝置列印包仍以電腦「習作批改」為主。

## 2026-08-16｜簡化開啟方式（GitHub Pages）

使用者被 raw.githack 英文中轉頁卡住。改為：
- `docs/index.html` 繁中入口（一顆「打開習作批改」）
- `docs/math-grader/` 同步最新手機批改
- Actions 自動部署 Pages：推送後網址預計 `https://copyshae.github.io/-/`

## 2026-08-16｜手機批改加速（效率）

現象：手機版自動批偏慢、步驟多。

優化（同分支 `cursor/mobile-math-grader-auto-b582`）：
- 預設「快速批」：精簡提示、`gemini-2.5-flash-lite`、限制輸出長度
- 匯入時壓縮大圖（最長邊 1600、JPEG）
- 連續批：答案 base64 只編一次；快速模式**雙線並行**
- 檔名座號自動對應；頂部「一鍵連續批」；螢幕喚醒鎖
- SW → `math-grader-v9`

## 2026-08-16｜手機習作批改卡住「批改中」

現象：手機「批改進度」00 試發整天停在批改中，01–35 未批，提示預覽空白。

根因：舊手機版**不會呼叫 Gemini API**，只複製提示／開網頁；按「貼上批」會標成批改中卻沒有完成回寫。

修復（分支 `cursor/mobile-math-grader-auto-b582`）：
- 手機設定 AI Studio API 金鑰（localStorage）
- 可選匯入正確答案；匯入試卷後 **Gemini自動批／連續自動批**
- 批完自動標「已批」並寫入回覆；「批改中→未批」一鍵解除卡住
- SW cache → `math-grader-v7`

正式 Pages 仍需本機 `apply-to-hello-world.ps1`／`pull-export-from-dash-repo.ps1` 套到 `hello-world`。
預覽：`https://raw.githack.com/copyshae/-/cursor/mobile-math-grader-auto-b582/_export/hello-world/directory/apps/math-grader/index.html`


## 2026-08-15｜習作台（teacher-desk）

線上頁：https://copyshae.github.io/hello-world/directory/apps/teacher-desk/

### 本日強化（匯出在 `_export/hello-world/`，待套用）

- 手機：檔名自動帶座號（`05-R01`）、PDF 提示、縮圖 URL 釋放、favicon／PWA meta、SW cache v5
- 桌面：接上既有 `掃描匯入` 資料夾 →「處理掃描匯入」複製到 `練習回傳` 並標待回；補回班級資料匯出／匯入、已發→待回
- **介面全面繁體中文**：Classroom→Google 教室、CS→掃描王、群發文按鈕去英文品牌堆疊；桌面設 zh-TW、程度／發送標籤
- 說明與 rule：繁中通則、座號-R次數命名、手機↔電腦同步

### 套用

見 `_export/hello-world/README.md` 的 `apply-to-hello-world.ps1`。

### 為何手機正式網址還沒變？

Cloud Agent **不能推** `copyshae/hello-world`。正式頁 https://copyshae.github.io/hello-world/directory/apps/teacher-desk/ 仍是舊版。

- 預覽（推上本分支後）：見 PR 說明的 raw.githack 連結
- 正式上線：本機跑 `_export/hello-world/apply-to-hello-world.ps1`


## 2026-08-15｜本機套用失敗（路徑＋舊 install 語法錯）

現象：
1. 在 `Desktop\hello-world` 跑 `.\ _export\hello-world\apply-to-hello-world.ps1` → **找不到**（`_export` 只在 `-` 倉庫）
2. 舊版 `install-desktop-apps.ps1` 第 32 行字串沒閉合 → TerminatorExpectedAtEndOfString

解法：在桌面 hello-world 用 `pull-export-from-dash-repo.ps1` 從 GitHub 分支下載修復檔並重裝。
見 `_export/hello-world/README.md` 的「套用（推薦）」整段指令。


根因：
1. `gemini-2.0-flash` 已於 2026-06-01 下線
2. 「網頁批閱」被誤當自動（其實要手動貼）
3. 自動批需 AI Studio API 金鑰（≠ 網頁 Pro）

行為（`math-homework-grader-app.ps1`）：
- 預設模型 → `gemini-2.5-flash`；預設模式 → `gemini_auto`
- **有正確答案 → 對照批**；**無正確答案 → 直接 AI 批**（都不擋、都自動）
- **Gemini自動批**／**連續自動批**：API 填分＋存註記
- 手機版（2026-08-16 起）同樣可 API 自動批；舊「貼上批」仍是手動

套用：`apply-to-hello-world.ps1` → `install-desktop-apps.ps1`  
流程：Gemini金鑰 →（可選）載入正確答案 → Gemini自動批／連續自動批
